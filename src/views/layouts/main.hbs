<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sàn Đấu Giá Online</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Local stylesheet -->
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>

    <!-- Header / Menu -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fa-solid fa-gavel"></i> Auction Pro
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
                aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNav">
                <ul class="navbar-nav primary-nav ms-3 mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Trang chủ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products">Sản phẩm</a>
                    </li>
                    <li class="nav-item dropdown category-dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="categoriesDropdown" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            Danh mục
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="categoriesDropdown">
                            {{#each layoutCategories}}
                            <li class="dropdown-submenu">
                                <a class="dropdown-item dropdown-toggle"
                                    href="/products?category={{this.id}}">{{this.name}}</a>
                                {{#if this.children.length}}
                                <ul class="dropdown-menu">
                                    {{#each this.children}}
                                    <li><a class="dropdown-item" href="/products?category={{this.id}}">{{this.name}}</a>
                                    </li>
                                    {{/each}}
                                </ul>
                                {{/if}}
                            </li>
                            {{else}}
                            <li><a class="dropdown-item" href="#">Chưa có danh mục</a></li>
                            {{/each}}
                        </ul>
                    </li>
                </ul>

                <!-- Search -> center on large screens -->
                <form class="d-flex header-search mx-lg-auto my-2 my-lg-0" role="search" action="/search" method="GET">
                    <input name="q" class="form-control me-2" type="search" placeholder="Tìm sản phẩm, danh mục..."
                        aria-label="Search">
                    <button class="btn btn-outline-light" type="submit"><i class="fa fa-search"></i></button>
                </form>

                <!-- Auth area -->
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    {{#if isAuth}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userMenuTop"
                            role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{authUser.avatarUrl}}" class="avatar me-2"
                                onerror="this.src='/images/avatar-placeholder.png'"> {{authUser.full_name}}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userMenuTop">
                            <li><a class="dropdown-item" href="/account">Tài khoản</a></li>
                            <li><a class="dropdown-item" href="/auth/logout">Đăng xuất</a></li>
                        </ul>
                    </li>
                    {{else}}
                    <li class="nav-item"><a class="nav-link" href="/auth/login">Đăng nhập</a></li>
                    <li class="nav-item"><a class="nav-link ms-2" href="/auth/register">Đăng ký</a></li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Body -->
    <div class="container">
        {{{body}}}
    </div>

    <!-- Footer -->
    <footer class="text-center mt-4 py-4 border-top">
        <p>&copy; 2025 Online Auction Project</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>